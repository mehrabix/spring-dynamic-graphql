package com.example.graphql.dto;

/**
 * Enum representing different timeframe types for reports
 */
public enum TimeframeType {
    DAILY,
    WEEKLY,
    MONTHLY,
    QUARTERLY,
    YEARLY,
    CUSTOM
} 